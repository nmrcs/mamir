DATABASE_URL=postgresql://mamir:mamir@localhost:5432/mamir
PORT=3001

# Scoring service (apps/scoring)
SCORING_URL=http://127.0.0.1:8001

# Plugins loaded at startup — comma-separated.
# The core validates each contract and fails if one is broken.
PLUGINS=@mamir/plugin-credit-risk,@mamir/plugin-payment-fraud
